# Infisical Machine Identity Credentials for DEV environment
# ⚠️ DO NOT COMMIT THIS FILE - Add to .gitignore
# Replace these values with your actual credentials from Infisical

# Machine Identity: ai-sentinel-dev
INFISICAL_DEV_CLIENT_ID=changeme
INFISICAL_DEV_CLIENT_SECRET=changeme

# Infisical Server Configuration
# ⚠️ CRITICAL: Change these secrets in production!
INFISICAL_ENCRYPTION_KEY=changeme
INFISICAL_AUTH_SECRET=changeme

# Infisical Configuration (usually no need to change these)
INFISICAL_API_URL=http://infisical:8082
INFISICAL_PROJECT_ID=changeme
INFISICAL_ENV=changeme

# Instructions:
# 1. Go to Infisical UI: http://localhost:8082
# 2. Navigate to your project → Settings → Access Control → Machine Identities
# 3. Find "ai-sentinel-dev" machine identity
# 4. Copy the Client ID and Client Secret
# 5. Replace the values above
# 6. Save this file
